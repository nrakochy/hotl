/target
.DS_Store

# Private planning/design corpus — kept on disk for local agents, never
# published. Held out of this repo and its history on purpose.
/AGENTS.md
/specs/

# Local agent scratch: machine-local settings and agent worktrees, the
# latter carrying their own target/ dirs. Nothing here is shared state.
/.claude/

# Nix build symlinks and the direnv cache. flake.nix and flake.lock ARE
# tracked — these are the per-checkout byproducts of using them.
/result
/result-*
/.direnv/
